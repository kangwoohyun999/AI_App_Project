# AI PsyChat Expo App
## Quick start
1. Install dependencies: `npm install` (or `yarn`)
2. Start Expo: `npm start`
3. The app expects a server running (see server/). Update API base URL in client/services/api.js
